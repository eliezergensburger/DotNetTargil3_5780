<UserControl x:Class="WpfTargil3.HostingUnitUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfTargil3"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="UserControlGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="tbUnitName" Text="{Binding Path=Unitname}" Margin="20,60,66.667,348" FontFamily="Tahoma" FontSize="20" />
        <Label Content="מספר חדרים" HorizontalAlignment="Left" Margin="124,206,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.627,4.314" FontFamily="Tahoma" FontSize="18"/>
        <TextBox HorizontalAlignment="Left" Height="39" Margin="35,199,0,0" TextWrapping="Wrap" Text="{Binding Path=Rooms}" VerticalAlignment="Top" Width="60"/>
        <CheckBox Content="בריכה" HorizontalAlignment="Left" Margin="35,288,0,0" VerticalAlignment="Center" IsChecked=
"{Binding Path=IsSwimingPool}" FontFamily="Tahoma" FontSize="20"/>
    </Grid>
</UserControl>
